# Application
PROJECT_NAME=AI Virtual Intern Platform (Dev)
ENVIRONMENT=development
DEBUG=True
SECRET_KEY=dev-secret-key-not-for-production

# Database
POSTGRES_DB=ai_intern_dev
POSTGRES_USER=postgres
POSTGRES_PASSWORD=dev-password

# AI Services (use development keys)
OPENAI_API_KEY=your-dev-openai-api-key

# AWS (use development bucket)
AWS_ACCESS_KEY_ID=your-dev-aws-access-key
AWS_SECRET_ACCESS_KEY=your-dev-aws-secret-key
AWS_BUCKET_NAME=ai-intern-platform-dev-files

# Email (use testing service)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=test@localhost
SMTP_PASSWORD=test
